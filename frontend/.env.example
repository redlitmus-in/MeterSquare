# Frontend Environment Variables - EXAMPLE FILE
# Copy this to .env and fill in your actual values
# NEVER commit .env to git!

# Backend API URL
VITE_API_URL=http://localhost:5000  # Development
# VITE_API_URL=https://api.yourdomain.com  # Production

# Supabase Configuration (Public Keys Only)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE

# ===============================================
# ⚠️ CRITICAL SECURITY WARNING
# ===============================================
# DO NOT add SUPABASE_SERVICE_ROLE_KEY here!
# Service role key gives full database access and
# should ONLY be in backend/.env
#
# Frontend .env files are bundled into JavaScript
# and visible to users, even if obfuscated!
# ===============================================

# Application Configuration
VITE_APP_NAME=MeterSquare ERP
VITE_APP_VERSION=1.0.0

# Environment
VITE_ENVIRONMENT=development  # development | production

# ===============================================
# SECURITY NOTES:
# ===============================================
# 1. Only put PUBLIC keys here (anon key, not service role key)
# 2. All VITE_ variables are exposed to browser
# 3. Never store secrets, API keys, or passwords here
# 4. Enable Supabase Row Level Security (RLS) policies
# 5. Add .env to .gitignore
# ===============================================
