# Backend Environment Variables - EXAMPLE FILE
# Copy this to .env and fill in your actual values
# NEVER commit .env to git!

# Environment
ENVIRONMENT=development  # development | production

# Server Configuration
PORT=5000
SECRET_KEY=GENERATE_A_SECURE_256_BIT_SECRET_HERE  # Use: openssl rand -base64 64

# Database Configuration (PostgreSQL/Supabase)
DATABASE_URL=postgresql://username:password@host:port/database_name
# Example: postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres

# Supabase Configuration (Backend Only)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=YOUR_SUPABASE_ANON_KEY_HERE
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE  # NEVER expose in frontend!

# Email Configuration (Gmail SMTP)
SENDER_EMAIL=your-email@gmail.com
SENDER_EMAIL_PASSWORD=your_gmail_app_password_here  # Generate at: https://myaccount.google.com/apppasswords

# Redis Configuration (Optional - for caching and rate limiting)
REDIS_URL=redis://localhost:6379/0  # Leave empty to use in-memory cache

# Database Connection Pool (Optional - defaults are optimized)
USE_SMALL_POOL=false  # Set to true for connection-limited databases like Supabase free tier

# Logging
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# Security (Production)
# SESSION_COOKIE_SECURE=True  # Uncomment for production (requires HTTPS)
# SESSION_COOKIE_SAMESITE=Strict  # Uncomment for production

# ===============================================
# SECURITY NOTES:
# ===============================================
# 1. Generate SECRET_KEY: openssl rand -base64 64
# 2. Use strong database password (16+ characters, mixed case, numbers, symbols)
# 3. NEVER expose SERVICE_ROLE_KEY in frontend
# 4. Enable 2FA on email account
# 5. Rotate credentials every 90 days
# 6. Set file permissions: chmod 600 .env
# 7. Add .env to .gitignore
# ===============================================
